package com.finpulse.enums;

/**
 * Represents the authentication provider used to register a user.
 * LOCAL = email/password registration
 * GOOGLE = Google OAuth2 login
 */
public enum AuthProvider {
    LOCAL,
    GOOGLE
}
